// ChatClient should be used for testing purposes only. This class hierarchy isn't directly 
// available but only as a base64-encoded string that consumers get when they download their 
// chat client scripts. See README for more info.
export { ChatClient } from './chat-client';

export { IConfig } from './config';
export { IUserlike } from './userlike';
