const BASE_URL = 'www.userlike.com/api/external/';

const BASE_URL_MESSAGE = `${BASE_URL}message/`;

const API_URLS = {
    offline_message: `${BASE_URL_MESSAGE}offline_message/`,
    chat_meta: `${BASE_URL_MESSAGE}chat_meta/`,
    operator: `${BASE_URL}operator/`
};

export {
    API_URLS
}
